<%= provide(:title, t("category.list")) %>
<h1><%= t("category.list") %></h1>

<ul class="inline">
  <% @categories.each do |category| %>
    <li>
      <%= link_to category.name, admin_caterory_path(category) %>
    </li>
  <% end %>
</ul>
