<% provide(:title, t(".users.all")) %>
<h1><%= t(".users.all") %></h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<div class="page-center">
  <%= will_paginate %>
</div>
